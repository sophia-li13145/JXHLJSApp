<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Name="ThisPage"
    x:Class="IndustrialControlMAUI.Pages.OutgoingQualityDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:conv="clr-namespace:IndustrialControlMAUI.Converters"
    BackgroundColor="White"
    Title="质量管理系统">

    <!-- 局部样式：按钮/标题/分组标题 -->
    <ContentPage.Resources>
        <ResourceDictionary>
            <conv:NullOrEmptyToBoolInverseConverter x:Key="NullOrEmptyToBoolInverseConverter" />
            <conv:StringToNullableDecimalConverter x:Key="DecConv" />
            <conv:DecParseOnlyConverter x:Key="DecParseOnlyConv" />
            <conv:DefectLevelToColorConverter x:Key="DefectLevelToColor"/>
            <conv:IntGreaterThanZeroConverter x:Key="IntGT0" />
            <conv:IndexConverter x:Key="IndexConverter" />
            <Style x:Key="PrimaryBtn" TargetType="Button">
                <Setter Property="HeightRequest" Value="36" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="18,6" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="BackgroundColor" Value="#2F80ED" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>
            <Style x:Key="SuccessBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
                <Setter Property="BackgroundColor" Value="#27AE60" />
            </Style>
            <Style x:Key="SectionTitle" TargetType="Label">
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="#1F2937" />
                <Setter Property="Margin" Value="0,10,0,6" />
            </Style>
            <Style x:Key="FieldLabel" TargetType="Label">
                <Setter Property="TextColor" Value="#4B5563" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
                <Setter Property="HorizontalOptions" Value="Start" />
                <Setter Property="HorizontalTextAlignment" Value="Start" />
                <Setter Property="Padding" Value="0" />
            </Style>
            <Style x:Key="FieldValue" TargetType="Label">
                <Setter Property="TextColor" Value="#111827" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
                <Setter Property="HorizontalOptions" Value="Start" />
                <Setter Property="HorizontalTextAlignment" Value="Start" />
            </Style>
            <Style x:Key="Card" TargetType="Frame">
                <Setter Property="Padding" Value="12" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="HasShadow" Value="False" />
                <Setter Property="BorderColor" Value="#E5E7EB" />
            </Style>
            <!-- 只读展示小灰框 -->
            <Style x:Key="ReadonlyPill" TargetType="Frame">
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="HasShadow" Value="False" />
                <Setter Property="BackgroundColor" Value="#F3F4F6" />
                <Setter Property="BorderColor" Value="#E5E7EB" />
            </Style>
            <Style TargetType="Entry">
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="HeightRequest" Value="42" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="HorizontalTextAlignment" Value="Start" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*" RowSpacing="0">

        <!-- 顶部蓝条 -->
        <Grid Grid.Row="0"  HeightRequest="56" Padding="12" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
            <Label Text="质检单基础信息"
                   VerticalOptions="Start"
                   TextColor="Black"
                   FontSize="14"
                   FontAttributes="Bold" />
            <Button Grid.Column="1"
                    Text="保存"
                    Style="{StaticResource PrimaryBtn}"
                    Command="{Binding SaveCommand}" IsVisible="{Binding IsEditing}" />
            <Button Grid.Column="2"
                    Text="完成质检"
                    Style="{StaticResource SuccessBtn}"
                    Command="{Binding CompleteCommand}" IsVisible="{Binding IsEditing}" />
        </Grid>


        <!-- 主体滚动 -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="10" Padding="12">

                <!-- 基础信息卡片 -->
                <Frame Style="{StaticResource Card}">
                    <Grid ColumnDefinitions="60,*,60,*"
          RowDefinitions="Auto,Auto,Auto,Auto,Auto"
          ColumnSpacing="10" RowSpacing="8">

                        <Label Grid.Row="0" Grid.Column="0" Text="质检单号：" Style="{StaticResource FieldLabel}" />
                        <Label Grid.Row="0" Grid.Column="1" Text="{Binding Detail.qualityNo}" Style="{StaticResource FieldValue}" />

                        <Label Grid.Row="0" Grid.Column="2" Text="质检单类型：" Style="{StaticResource FieldLabel}" />
                        <Label Grid.Row="0" Grid.Column="3" Text="{Binding Detail.qualityTypeName}" Style="{StaticResource FieldValue}" />

                        <Label Grid.Row="1" Grid.Column="0" Text="关联工单号：" Style="{StaticResource FieldLabel}" />
                        <Label Grid.Row="1" Grid.Column="1" Text="{Binding Detail.orderNumber}" Style="{StaticResource FieldValue}" />

                        <!-- 第三行 -->
                        <Label Grid.Row="1" Grid.Column="2" Text="产品名称：" Style="{StaticResource FieldLabel}" />
                        <Label Grid.Row="1" Grid.Column="3" Text="{Binding Detail.orderQualityMaterial.materialName}" Style="{StaticResource FieldValue}" />

                        <Label Grid.Row="2" Grid.Column="0" Text="生产数量：" Style="{StaticResource FieldLabel}" />
                        <Label Grid.Row="2" Grid.Column="1" Text="{Binding Detail.orderQualityMaterial.QtyWithUnit, StringFormat='{0:G29}'}" Style="{StaticResource FieldValue}" />
                        <Label Grid.Row="2" Grid.Column="2" Text="实际入库数量：" Style="{StaticResource FieldLabel}" />
                        <Label Grid.Row="2" Grid.Column="3" Text="{Binding Detail.orderQualityMaterial.InstockQtyWithUnit , StringFormat='{0:G29}'}" Style="{StaticResource FieldValue}" />

                        <Label Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" >
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="*" TextColor="Red" FontAttributes="Bold"/>
                                    <Span Text="质检员：" />
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                        <!-- 外层容器，便于定位下拉 -->
                        <Grid Grid.Row="3" Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 输入框：双向绑定 InspectorText -->
                            <Grid Grid.Row="0" ColumnDefinitions="*" VerticalOptions="Center">
                                <Entry
                    Text="{Binding InspectorText, Mode=TwoWay}"
                    Placeholder="输入"
                    Keyboard="Text"
                    IsTextPredictionEnabled="True"
                    IsSpellCheckEnabled="True"
                    ClearButtonVisibility="WhileEditing"
                    Completed="OnInspectorEntryCompleted" 
                    IsEnabled="{Binding IsEditing}"
                    />

                            </Grid>

                            <!-- 下拉建议列表 -->
                            <Border Grid.Row="1"
                    StrokeThickness="1"
                    Stroke="#DDD"
                    BackgroundColor="White"
                    Padding="0"
                    Margin="0,4,0,0"
                    IsVisible="{Binding IsInspectorDropdownOpen}">
                                <CollectionView
                    ItemsSource="{Binding InspectorSuggestions}"
                    SelectionMode="Single"
                    HeightRequest="200">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Padding="12">
                                                <Label Text="{Binding realname}" />
                                                <Grid.GestureRecognizers>
                                                    <TapGestureRecognizer
                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.PickInspectorCommand}"
                CommandParameter="{Binding .}" />
                                                </Grid.GestureRecognizers>
                                            </Grid>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </Border>
                        </Grid>

                        <Label Grid.Row="3" Grid.Column="2" Text="检验日期：" Style="{StaticResource FieldLabel}" />
                        <Label Grid.Row="3" Grid.Column="3" Text="{Binding Detail.inspectTime}" Style="{StaticResource FieldValue}" />
                    </Grid>
                </Frame>

                <!-- 检验结果（严格按行列） -->
                <Frame Style="{StaticResource Card}">
                    <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto"
                          ColumnDefinitions="50,*,60,*,60,*"
                          RowSpacing="5" ColumnSpacing="0"> 

                        <!-- 第0行：检验结果 下拉 -->
                        <Label Grid.Row="0" Grid.Column="0"  Grid.ColumnSpan="2"  Style="{StaticResource FieldLabel}" >
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="*" TextColor="Red" FontAttributes="Bold"/>
                                    <Span Text=" 检验结果: " />
                            </FormattedString>
                        </Label.FormattedText>
                        </Label>
                        <Picker Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2"
                                ItemsSource="{Binding InspectResultOptions}"
                                ItemDisplayBinding="{Binding Text}"
                                SelectedItem="{Binding SelectedInspectResult, Mode=TwoWay}"
                                IsEnabled="{Binding IsEditing}">
                            <Picker.Triggers>
                                <DataTrigger TargetType="Picker"
                       Binding="{Binding SelectedInspectResult.Text}"
                       Value="合格">
                                    <Setter Property="BackgroundColor" Value="#B2F2BB"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Picker"
                       Binding="{Binding SelectedInspectResult.Text}"
                       Value="不合格">
                                    <Setter Property="BackgroundColor" Value="#FFA8A8"/>
                                </DataTrigger>
                            </Picker.Triggers>

                        </Picker>

                        <BoxView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="6" HeightRequest="1" BackgroundColor="#E5E7EB" />
                        <!-- 第1行：抽样总数、不良总数、抽样不良率(只读) -->
                        <Label Grid.Row="2" Grid.Column="0" Text="抽样总数：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="2" Grid.Column="1"
                               Text="{Binding Detail.totalSampling, Mode=TwoWay, Converter={StaticResource DecParseOnlyConv}}"
                               Keyboard="Numeric"
                               IsEnabled="{Binding IsEditing}" />   

                        <Label Grid.Row="2" Grid.Column="2" Text="不良总数：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="2" Grid.Column="3"
                               Text="{Binding Detail.totalBad, Mode=TwoWay, Converter={StaticResource DecParseOnlyConv}}"
                               Keyboard="Numeric"
                               IsEnabled="{Binding IsEditing}" />

                        <Label Grid.Row="2" Grid.Column="4" Text="抽样不良率：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="2" Grid.Column="5"
                               Text="{Binding Detail.samplingDefectRate, Mode=OneWay, StringFormat='{0:0.##}%'}"
                               IsEnabled="False" /> 

                        <!-- 只读，显示XXXX风格 -->

                        <!-- 第2行：合格总数、不合格总数、总体合格率(只读) -->
                        <Label Grid.Row="3" Grid.Column="0" Text="合格总数：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="3" Grid.Column="1"
                               Text="{Binding Detail.totalQualified, Mode=TwoWay,  Converter={StaticResource DecParseOnlyConv}}"
                               Keyboard="Numeric"
                               IsEnabled="{Binding IsEditing}" /> 

                        <Label Grid.Row="3" Grid.Column="2" Text="不合格总数：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="3" Grid.Column="3"
                               Text="{Binding Detail.totalUnqualified, Mode=TwoWay,Converter={StaticResource DecParseOnlyConv}}"
                               Keyboard="Numeric"
                               IsEnabled="{Binding IsEditing}" /> 

                        <Label Grid.Row="3" Grid.Column="4" Text="总体合格率：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="3" Grid.Column="5"
                               Text="{Binding Detail.passRate, Mode=OneWay, StringFormat='{0:0.##}%'}"
                               IsEnabled="False" /> 
                        <!-- 只读 -->

                        <!-- 第3行：质检损耗、留样数量、留样编号 -->
                        <Label Grid.Row="4" Grid.Column="0" Text="质检损耗：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="4" Grid.Column="1"
                               Text="{Binding Detail.inspectionLossQty, Mode=TwoWay, Converter={StaticResource DecConv}}"
                               Keyboard="Numeric"
                               IsEnabled="{Binding IsEditing}" /> 

                        <Label Grid.Row="4" Grid.Column="2" Text="留样数量：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="4" Grid.Column="3"
                               Text="{Binding Detail.retainedSampleQty, Mode=TwoWay, Converter={StaticResource DecConv}}"
                               Keyboard="Numeric"
                               IsEnabled="{Binding IsEditing}" />

                        <Label Grid.Row="4" Grid.Column="4" Text="留样编号：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="4" Grid.Column="5"
                               Text="{Binding Detail.retainedSampleNumber, Mode=TwoWay}"
                               IsEnabled="{Binding IsEditing}" />   

                        <!-- 第4行：检验备注（整行） -->
                        <Label Grid.Row="5" Grid.Column="0" Text="检验备注：" Style="{StaticResource FieldLabel}" />
                        <Entry Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="5"
                               Text="{Binding Detail.inspectRemark, Mode=TwoWay}"
                               IsEnabled="{Binding IsEditing}" /> 
                    </Grid>
                </Frame>

                <!-- 质检详情列表 -->
                <Label Text="质检详情" Style="{StaticResource SectionTitle}" HorizontalOptions="Start" />

                <CollectionView ItemsSource="{Binding Items}" SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <!-- 左侧序号 + 右侧原卡片 -->
                            <Grid ColumnDefinitions="12,*" ColumnSpacing="5" Margin="0,6,0,0">
                                <!-- 序号：与卡片顶部对齐 -->
                                <Label Grid.Column="0"
                       Text="{Binding index}"
                       VerticalOptions="Center"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"
                       FontAttributes="Bold"
                       TextColor="#111827"
                       Margin="0,8,0,0"/>

                                <!-- 右侧你的原 Frame 卡片，内容保持不变 -->
                                <Frame Grid.Column="1" Style="{StaticResource Card}">
                                    <Grid
                        RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto"
                        ColumnDefinitions="Auto,*"
                        RowSpacing="8" ColumnSpacing="5">

                                        <!-- 行0：项目名称 -->
                                        <Label Grid.Row="0" Grid.Column="0" Text="项目名称：" Style="{StaticResource FieldLabel}" />
                                        <Label Grid.Row="0" Grid.Column="1" Text="{Binding inspectionName}" Style="{StaticResource FieldValue}" />

                                        <!-- 行1：检验属性、检验方式 -->
                                        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                              ColumnDefinitions="Auto,*,Auto,*" ColumnSpacing="10">
                                            <Label Grid.Column="0" Text="检验属性：" Style="{StaticResource FieldLabel}" />
                                            <Label Grid.Column="1" Text="{Binding inspectionAttributeName}" Style="{StaticResource FieldValue}" />
                                            <Label Grid.Column="2" Text="检验方式：" Style="{StaticResource FieldLabel}" />
                                            <Label Grid.Column="3" Text="{Binding inspectionMode}" Style="{StaticResource FieldValue}" />
                                        </Grid>

                                        <!-- 行2：标准值、上限值、下限值 -->
                                        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                              ColumnDefinitions="Auto,*,Auto,*,Auto,*" ColumnSpacing="10">
                                            <Label Grid.Column="0" Text="标准值：" Style="{StaticResource FieldLabel}" />
                                            <Label Grid.Column="1" Text="{Binding standardValue}" Style="{StaticResource FieldValue}" />
                                            <Label Grid.Column="2" Text="上限值：" Style="{StaticResource FieldLabel}" />
                                            <Label Grid.Column="3" Text="{Binding upperLimit}" Style="{StaticResource FieldValue}" />
                                            <Label Grid.Column="4" Text="下限值：" Style="{StaticResource FieldLabel}" />
                                            <Label Grid.Column="5" Text="{Binding lowerLimit}" Style="{StaticResource FieldValue}" />
                                        </Grid>

                                        <!-- 行3：检验结果 -->
                                        <!-- 行3：检验结果（明细项） -->
                                        <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                                ColumnDefinitions="Auto,*" ColumnSpacing="10">
                                            <Label Grid.Column="0" Text="检验结果：" Style="{StaticResource FieldLabel}" />

                                            <Picker x:Name="RowResultPicker"
                                                    Grid.Column="1"
                                                    ItemsSource="{Binding Source={x:Reference ThisPage}, Path=BindingContext.InspectResultTextList}"
                                                    SelectedItem="{Binding inspectResult, Mode=TwoWay}"
                                                    IsEnabled="{Binding Source={x:Reference ThisPage}, Path=BindingContext.IsEditing}"> 
                                                <!-- 默认底色（可选） -->
                                                <Picker.BackgroundColor>#F3F4F6</Picker.BackgroundColor>

                                                <Picker.Triggers>
                                                    <!-- 直接监听控件自己的 SelectedItem，避免依赖模型的通知 -->
                                                    <DataTrigger TargetType="Picker"
                         Binding="{Binding Source={x:Reference RowResultPicker}, Path=SelectedItem}"
                         Value="合格">
                                                        <Setter Property="BackgroundColor" Value="#B2F2BB"/>
                                                    </DataTrigger>

                                                    <DataTrigger TargetType="Picker"
                         Binding="{Binding Source={x:Reference RowResultPicker}, Path=SelectedItem}"
                         Value="不合格">
                                                        <Setter Property="BackgroundColor" Value="#FFA8A8"/>
                                                    </DataTrigger>
                                                </Picker.Triggers>
                                            </Picker>
                                        </Grid>


                                        <!-- 行4：抽样数、不良数、不良率(只读) -->
                                        <Grid Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                              ColumnDefinitions="Auto,*,Auto,*,Auto,*" ColumnSpacing="10">
                                            <Label Grid.Column="0" Text="抽样数：" Style="{StaticResource FieldLabel}" />
                                            <Entry Grid.Column="1" Text="{Binding sampleQty, Mode=TwoWay, Converter={StaticResource DecConv}}" Keyboard="Numeric"
                                   IsEnabled="{Binding Source={x:Reference Name=ThisPage}, Path=BindingContext.IsEditing}" />
                                            <Label Grid.Column="2" Text="不良数：" Style="{StaticResource FieldLabel}" />
                                            <Entry Grid.Column="3" Text="{Binding badQty, Mode=TwoWay, Converter={StaticResource DecConv}}" Keyboard="Numeric"
                                   IsEnabled="{Binding Source={x:Reference Name=ThisPage}, Path=BindingContext.IsEditing}" />
                                            <Label Grid.Column="4" Text="不良率：" Style="{StaticResource FieldLabel}" />
                                            <Frame Grid.Column="5" Padding="0,0" CornerRadius="6" HasShadow="False"
       BackgroundColor="#F3F4F6" BorderColor="#E5E7EB"> 
                                                <Label Text="{Binding badRate, StringFormat='{0:0.##}%'}"
           VerticalTextAlignment="Center" />
                                            </Frame>

                                        </Grid>

                                        <!-- 行5：检验设备 + 检验参数 -->
                                        <Grid Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                              ColumnDefinitions="Auto,*,Auto,*" ColumnSpacing="10">
                                            <Label Grid.Column="0" Text="检验设备：" Style="{StaticResource FieldLabel}" />
                                            <Picker Grid.Column="1"
                                    Title="请选择"
                                    ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.InspectDeviceList}"
                                    ItemDisplayBinding="{Binding Name}"
                                    SelectedItem="{Binding selectedInspectDevice, Mode=TwoWay}"
                                    IsEnabled="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.IsEditing}"
                                    BackgroundColor="#F3F4F6" />

                                            <Label Grid.Column="2" Text="检验参数：" Style="{StaticResource FieldLabel}" />
                                            <Picker Grid.Column="3"
                                    Title="请选择"
                                    ItemsSource="{Binding InspectParamOptions}"
                                    ItemDisplayBinding="{Binding Name}"
                                    SelectedItem="{Binding selectedInspectParam, Mode=TwoWay}"
                                    IsEnabled="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.IsEditing}"
                                    BackgroundColor="#F3F4F6" />
                                        </Grid>

                                        <!-- 行6：开始时间 + 结束时间（方案2：点击弹窗选日期+时间，可清空） -->
                                        <Grid Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
      ColumnDefinitions="Auto,*,Auto,*" ColumnSpacing="10">

                                            <Label Grid.Column="0" Text="开始时间：" Style="{StaticResource FieldLabel}" />

                                            <!-- 开始时间：灰框 + 点击 -->
                                            <Frame Grid.Column="1"
           Style="{StaticResource ReadonlyPill}"
           Padding="10,8">
                                                <Frame.GestureRecognizers>
                                                    <TapGestureRecognizer
                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.PickStartDateTimeCommand}"
                CommandParameter="{Binding .}" />
                                                </Frame.GestureRecognizers>

                                                <Label Text="{Binding inspectStartTime, TargetNullValue=请选择}"
               TextColor="#111827"
               VerticalTextAlignment="Center" />
                                                <Frame.IsEnabled>
                                                    <Binding Source="{RelativeSource AncestorType={x:Type ContentPage}}"
                     Path="BindingContext.IsEditing"/>
                                                </Frame.IsEnabled>
                                            </Frame>

                                            <Label Grid.Column="2" Text="结束时间：" Style="{StaticResource FieldLabel}" />

                                            <!-- 结束时间：灰框 + 点击 -->
                                            <Frame Grid.Column="3"
           Style="{StaticResource ReadonlyPill}"
           Padding="10,8">
                                                <Frame.GestureRecognizers>
                                                    <TapGestureRecognizer
                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.PickEndDateTimeCommand}"
                CommandParameter="{Binding .}" />
                                                </Frame.GestureRecognizers>

                                                <Label Text="{Binding inspectEndTime, TargetNullValue=请选择}"
               TextColor="#111827"
               VerticalTextAlignment="Center" />
                                                <Frame.IsEnabled>
                                                    <Binding Source="{RelativeSource AncestorType={x:Type ContentPage}}"
                     Path="BindingContext.IsEditing"/>
                                                </Frame.IsEnabled>
                                            </Frame>
                                        </Grid>

                                        <!-- 行7：实际值 + 两个按钮 -->
                                        <Grid Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2"
                              ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                                            <Label Grid.Column="0" Text="实际值：" Style="{StaticResource FieldLabel}" />
                                            <Entry Grid.Column="1"
                                   Placeholder="请输入"
                                   Text="{Binding actualValue, Mode=TwoWay, Converter={StaticResource DecConv}}"
                                   Keyboard="Numeric"
                                   IsEnabled="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.IsEditing}" />

                                            <HorizontalStackLayout Grid.Column="2" Spacing="8">
                                                <Button Text="自动检验"
                                        BackgroundColor="#22C55E"
                                        TextColor="White"
                                        CornerRadius="6"
                                        Padding="10,6"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.AutoInspectCommand}"
                                        CommandParameter="{Binding .}"
                                        IsEnabled="{Binding IsAutoInspectEnabled}"/>
                                                <Button Text="查看检验详细数据"
                                        BackgroundColor="Transparent"
                                        TextColor="#2563EB"
                                        Padding="6,6"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ViewInspectDetailCommand}"
                                        CommandParameter="{Binding .}"/>
                                            </HorizontalStackLayout>
                                        </Grid>

                                        <!-- 行8：不良原因 -->
                                        <Grid Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2"
                              ColumnDefinitions="Auto,*" ColumnSpacing="10">
                                            <Label Grid.Column="0" Text="不良原因：" Style="{StaticResource FieldLabel}" />
                                            <Entry Grid.Column="1" Text="{Binding badCause, Mode=TwoWay}"
                                   IsEnabled="{Binding Source={x:Reference Name=ThisPage}, Path=BindingContext.IsEditing}" />
                                        </Grid>

                                        <!-- 行9：缺陷 + 新增 -->
                                        <Grid Grid.Row="9" Grid.Column="0" ColumnDefinitions="Auto,*" ColumnSpacing="0" Margin="0,0,0,0">
                                            <Label Text="缺陷：" Grid.Column="0" VerticalTextAlignment="Center" />

                                            <!-- 标签区：只读显示已选缺陷 -->
                                            <HorizontalStackLayout Grid.Column="1" Spacing="6">
                                                <FlexLayout BindableLayout.ItemsSource="{Binding SelectedDefects}"
                Direction="Row" Wrap="Wrap" AlignItems="Center">
                                                    <BindableLayout.ItemTemplate>
                                                    <DataTemplate>
                                                        <Frame Padding="8,4"
                                                               Margin="4"
                                                               HasShadow="False"
                                                               CornerRadius="6"
                                                               BackgroundColor="{Binding ColorHex}"> 
                                                            <Label Text="{Binding Name}" FontSize="12" TextColor="Black"/>
                                                        </Frame>
                                                    </DataTemplate>
                                                </BindableLayout.ItemTemplate>
                                            </FlexLayout>



                                            <!-- “+”按钮：打开缺陷选择弹窗 -->
                                            <Button 
                                                    Text="＋"
                                                    WidthRequest="36"
                                                    HeightRequest="28"
                                                    FontSize="18"
                                                    TextColor="#111111"
                                                    BackgroundColor="White"
                                                    BorderColor="#D1D5DB"
                                                    BorderWidth="1"
                                                    CornerRadius="6"
                                                    Padding="0"
                                                    HorizontalOptions="Center"
                                                    VerticalOptions="Center"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenDefectPickerCommand}"
                                                    CommandParameter="{Binding .}" 
                                                    IsVisible="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.IsEditing}"/>
                                            </HorizontalStackLayout>

                                        </Grid>
                                    </Grid>
                                </Frame>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- 图片附件 -->
                <Label Text="图片附件" Style="{StaticResource SectionTitle}" HorizontalOptions="Start" />
                <!-- 左窄右宽两列 -->
                <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto" ColumnDefinitions="40,*" RowSpacing="8">

                    <!-- Row0 左：图片上传标题 -->
                    <Label Grid.Row="0" Grid.Column="0" Text="图片上传：" VerticalTextAlignment="Start" />

                    <!-- Row0 右：缩略图 + 上传方块 -->
                    <HorizontalStackLayout Grid.Row="0" Grid.Column="1" Spacing="8" VerticalOptions="Start">

                        <CollectionView ItemsSource="{Binding ImageAttachments}"
                    ItemsLayout="HorizontalList"
                    HeightRequest="96"
                    SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Frame Padding="0" WidthRequest="96" HeightRequest="96"
                   CornerRadius="8" HasShadow="False" BorderColor="#E5E7EB" Margin="0,0,8,0">
                                            <Image Aspect="AspectFill" Source="{Binding DisplaySource}" />
                                        </Frame>
                                        <!-- 右上角删除 -->
                                        <Button Text="×" FontSize="12" Padding="0"
                    WidthRequest="22" HeightRequest="22" CornerRadius="11"
                    BackgroundColor="#00000066" TextColor="White"
                    HorizontalOptions="End" VerticalOptions="Start" Margin="2"
                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteAttachmentCommand}"
                    CommandParameter="{Binding .}" 
                    IsVisible="{Binding IsEditing}"                         />
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <!-- 上传方块 -->
                        <Frame WidthRequest="96" HeightRequest="96" CornerRadius="8"
           BorderColor="#E5E7EB" BackgroundColor="White"
           HasShadow="False" Padding="8" VerticalOptions="Start" IsVisible="{Binding IsEditing}" >
                            <VerticalStackLayout Spacing="4" VerticalOptions="Center" HorizontalOptions="Center">
                                <Label Text="＋" FontSize="28" TextColor="#9CA3AF" />
                                <Label Text="单击上传" FontSize="12" TextColor="#9CA3AF" Margin="0,0,0,2" LineHeight="1.1"/>
                            </VerticalStackLayout>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer  Tapped="OnPickImagesClicked" />
                            </Frame.GestureRecognizers>
                        </Frame>
                    </HorizontalStackLayout>

                    <!-- Row1 左：附件信息标题 -->
                    <Label Grid.Row="1" Grid.Column="0" Text="附件信息：" VerticalTextAlignment="Center" />

                    <!-- Row1 右：上传附件链接 -->
                    <HorizontalStackLayout Grid.Row="1" Grid.Column="1" Spacing="12" VerticalOptions="Center" IsVisible="{Binding IsEditing}">
                        <Label Text="上传附件" TextColor="#3B82F6">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnPickFileClicked" />
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>

                    <!-- Row2 右：提示 -->
                    <Label Grid.Row="2" Grid.Column="1"
         Text="（单个附件最大支持20M，支持格式PDF、Word、Excel、Txt、JPG、PNG、RAR）"
         FontSize="12" TextColor="#9CA3AF" IsVisible="{Binding IsEditing}" />

                    <!-- Row3 右：附件表格 -->
                    <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" RowDefinitions="Auto,*" Margin="0,4,0,0">
                        <Grid Grid.Row="0" ColumnDefinitions="40,100,40,80,120"
          BackgroundColor="#E5EEF9" Padding="8,8">
                            <Label Grid.Column="0" Text="序号" />
                            <Label Grid.Column="1" Text="附件名称" />
                            <Label Grid.Column="2" Text="大小" />
                            <Label Grid.Column="3" Text="上传时间" />
                            <Label Grid.Column="4" Text="操作" />
                        </Grid>

                        <CollectionView x:Name="AttList" Grid.Row="1" ItemsSource="{Binding Attachments}" SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnDefinitions="40,100,40,80,120" Padding="8,10">
                                        <Label Text="{Binding .,
                                      Converter={StaticResource IndexConverter},
                                      ConverterParameter={x:Reference AttList}}" />
                                        <Label Grid.Column="1" Text="{Binding AttachmentName}" LineBreakMode="TailTruncation"/>
                                        <Label Grid.Column="2" Text="{Binding AttachmentSize}" />
                                        <Label Grid.Column="3" Text="{Binding CreatedTime}" />
                                        <HorizontalStackLayout Grid.Column="4" Spacing="16">
                                            <Label Text="下载" TextColor="#3B82F6">
                                                <Label.GestureRecognizers>
                                                    <TapGestureRecognizer
                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DownloadAttachmentCommand}"
                    CommandParameter="{Binding .}" />
                                                </Label.GestureRecognizers>
                                            </Label>
                                            <Label Text="删除" TextColor="Red" IsVisible="{Binding IsEditing}">
                                                <Label.GestureRecognizers>
                                                    <TapGestureRecognizer
                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteAttachmentCommand}"
                    CommandParameter="{Binding .}" />
                                                </Label.GestureRecognizers>
                                            </Label>
                                        </HorizontalStackLayout>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </Grid>

                    <Grid Grid.Row="4"  Grid.Column="0" Grid.ColumnSpan="2" RowDefinitions="Auto,*">
                        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
ColumnDefinitions="Auto,*,Auto,*" ColumnSpacing="10">
                            <Label Grid.Column="0" Text="创建人：" Style="{StaticResource FieldLabel}" />
                            <Label Grid.Column="1" Text="{Binding Detail.creator}" Style="{StaticResource FieldValue}" />
                            <Label Grid.Column="2" Text="创建日期：" Style="{StaticResource FieldLabel}" />
                            <Label Grid.Column="3" Text="{Binding Detail.createdTime}" Style="{StaticResource FieldValue}" />
                        </Grid>
                        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
ColumnDefinitions="Auto,*,Auto,*" ColumnSpacing="10">
                            <Label Grid.Column="0" Text="最近修改人：" Style="{StaticResource FieldLabel}" />
                            <Label Grid.Column="1" Text="{Binding Detail.modifier}" Style="{StaticResource FieldValue}" />
                            <Label Grid.Column="2" Text="修改日期：" Style="{StaticResource FieldLabel}" />
                            <Label Grid.Column="3" Text="{Binding Detail.modifiedTime}" Style="{StaticResource FieldValue}" />
                        </Grid>
                    </Grid>
                </Grid>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
